import styled from "styled-components";

export const ArtitsCardsMainContainer = styled.div`
	display: flex;
	flex-direction: column;
	width: 100%;
	flex-wrap: wrap;
	.recent-searches-container{
		color: white;
		h1{
			color: white;
			font-family: system-ui;
			font-weight: normal;
			font-size: 4rem;
		}
	}
	.no-results-container{
		color:white;
	}
`